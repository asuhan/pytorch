graph(%0 : Double(2!, 5)) {
  %1 : Dynamic = prim::GradOf[name="aten::expand"](%0)
    block0() {
      %2 : int[] = prim::Constant[value=[0]]()
      %3 : bool = prim::Constant[value=0]()
      %4 : Dynamic = aten::sum(%0, %2, %3)
      -> (%4)
    }
  return (%1);
}
